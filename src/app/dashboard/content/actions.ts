'use server';

// The logic from this file has been moved to the relevant client components
// (ContentForm.tsx and Columns.tsx) to resolve an authentication issue
// where server actions were not being authenticated with Firebase.
